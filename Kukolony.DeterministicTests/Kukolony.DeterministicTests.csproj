<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup><OutputType>Exe</OutputType><TargetFramework>net10.0</TargetFramework><ImplicitUsings>enable</ImplicitUsings><Nullable>enable</Nullable></PropertyGroup>
  <!--
    Pure decision logic is linked in here, without Unity, so a whole behaviour cycle is
    verified in about a second rather than only inside a four-minute game run. There is
    nothing to link at the moment: the job system that used this was removed with the rest
    of the feature layer, and gets rebuilt at roadmap milestone 6. The wiring stays so the
    first pure logic written after that is covered immediately.
  -->
</Project>
