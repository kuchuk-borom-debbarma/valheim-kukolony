namespace Kukolony.Colonies
{
    /// <summary>
    ///     Colony members are people. Placed objects live in the separate typed structure
    ///     registry, so beds, containers and stations can never enter this list.
    /// </summary>
    internal enum ColonyMemberKind
    {
        Villager
    }
}
